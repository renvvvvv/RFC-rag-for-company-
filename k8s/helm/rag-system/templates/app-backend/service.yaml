{{- if .Values.appBackend.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-system.fullname" . }}-app-backend
  labels:
    {{- include "rag-system.labels" . | nindent 4 }}
    {{- include "rag-system.componentLabels" (dict "component" "app-backend") | nindent 4 }}
spec:
  type: {{ .Values.appBackend.service.type }}
  ports:
    - port: {{ .Values.appBackend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rag-system.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app-backend
{{- end }}
